kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.numtracker.storage.pvc_name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.numtracker.storage.size }}
  persistentVolumeReclaimPolicy: Retain
