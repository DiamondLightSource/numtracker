kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.numtracker.storage.pvcName }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.numtracker.storage.size }}
